1. Folder AFL contains:
 - "fuzzme.c", which is the test driver for fuzzMe
 - "out" folder, which is the output from AFL
 - "in" folder, which is the initial seed for AFL

2. Folder KLEE contains:
 - "fuzzme.c", which is the KLEE test driver for fuzzMe
 - "out" folder, which is the output from KLEE
